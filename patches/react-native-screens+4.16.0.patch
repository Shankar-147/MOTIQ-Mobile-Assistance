diff --git a/node_modules/react-native-screens/android/build.gradle b/node_modules/react-native-screens/android/build.gradle
index 114be81..db410d1 100644
--- a/node_modules/react-native-screens/android/build.gradle
+++ b/node_modules/react-native-screens/android/build.gradle
@@ -138,6 +138,10 @@ android {
         externalNativeBuild {
             cmake {
                 arguments "-DANDROID_STL=c++_shared",
+                        // See react-native-worklets/android/build.gradle's identical
+                        // comment: this machine's CMake drops -lc++_shared from the
+                        // generated link line for this NDK, forced back explicitly.
+                        "-DCMAKE_SHARED_LINKER_FLAGS=-lc++_shared",
                         "-DRNS_NEW_ARCH_ENABLED=${IS_NEW_ARCHITECTURE_ENABLED}",
                         "-DANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES=ON"
             }
@@ -150,6 +154,11 @@ android {
     externalNativeBuild {
         cmake {
             path "CMakeLists.txt"
+            // Force CMake 3.31.6 (ninja 1.12.1) — see expo-modules-core's
+            // build.gradle comment; old ninja 1.10.2 (bundled with CMake
+            // 3.22.1) has Windows path-handling bugs that were silently
+            // dropping libc++_shared linker args on this machine.
+            version "3.31.6"
         }
     }
     lintOptions {
