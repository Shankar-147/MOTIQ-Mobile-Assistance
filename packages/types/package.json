{
  "name": "@motiq/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript enums and DTOs, mirroring docs/domain-model.md. Single source of truth for the API contract shape.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
